
# path to the input data in excel format
# the format should follow the example in assets/input_example.xlsx
# with the same columns and the same order
# each stream must have a different name, as it is used to identify it
# if None, the assets/input_example.xlsx is used
# should be something like: r"path-to-parent-directory\directory-name\file-name.xlsx"
path_to_input = None

# if None, the path_to_input directory is used
# should be something like: r"path-to-parent-directory\directory-name"
path_to_output_dir = None

